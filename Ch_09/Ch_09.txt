1. What is a relative path relative to?

	-> The current working directory.

2. What does an absolute path start with?

	-> The root folder 

3. What does Path('C:/Users') / 'Al' evaluate to on Windows?

	-> WindowsPath('C:/Users/Al')

4. What does 'C:/Users' / 'Al' evaluate to on Windows?

	-> It gives an error : unsupported operand type(s) for /: 'str' and 'str'
	because / operator cannot be used with strings.

5. What do the os.getcwd() and os.chdir() functions do?

	-> os.getcwd() gives the path of the current working directory.
	-> os.chdir() allows us to change the current working directory.

6. What are the . and .. folders?

	-> (.) is the current folder we are on and (..) is the parent folder of the current folder.

7. In C:\bacon\eggs\spam.txt, which part is the dir name, and which part is the base name?

	-> 'C:\bacon\eggs' is the dir name.
	-> 'spam.txt' is the base name.

8. What are the three “mode” arguments that can be passed to the open() function?

	-> read(r), write(w) and append(a).

9. What happens if an existing file is opened in write mode?

	-> It gets overwritten.

10. What is the difference between the read() and readlines() methods?

	-> read() gives all the text inside the text file as a single string.
	-> readlines() gives a list of strings with each string being each line of the text file.

11. What data structure does a shelf value resemble?

	-> A dictionary.

